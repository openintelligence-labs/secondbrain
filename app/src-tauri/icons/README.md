Icon files referenced by `tauri.conf.json`. Real assets land in T-06 (the
build pass); for now Tauri will use its built-in default icon when these
files are absent, which is fine for the dev preview.
